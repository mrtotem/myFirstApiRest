var mongoose = require('mongoose');
var Schema = mongoose.Schema;

var ArrivedSchema = new Schema({
	userId : String,
	name : String,
	message : String,
	date : String,
	lat : String,
	long : String,
	type : String
});

module.exports = mongoose.model('Arrived', ArrivedSchema);